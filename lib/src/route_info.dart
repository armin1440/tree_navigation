class RouteInfo {
  const RouteInfo({
    required this.path,
    // required this.name,
    required this.isShellRoute,
  });

  final String path;
  // final String name;
  final bool isShellRoute;
}
